After you setup ruby and rails use the following to start the server:
```
$ cd csc347cs
$ rake demo:setup
$ rake demo:setup RAILS_ENV='dummy'
$ rails s
```
Then navigate to [http://localhost:3000](http://localhost:3000)